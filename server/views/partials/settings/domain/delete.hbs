<div class="content">
  <h2>{{t "settings.delete_domain"}}</h2>
  <p>
    {{t "settings.delete_domain_confirm"}}
  </p>
  <div class="buttons">
    <button type="button" hx-on:click="closeDialog()">{{t "common.cancel"}}</button>
    <button 
      type="button"
      class="danger confirm" 
      hx-delete="/api/domains/{id}" 
      hx-ext="path-params" 
      hx-vals='{"id":"{{id}}"}' 
      hx-target="closest .content" 
      hx-swap="none" 
      hx-indicator="closest .content"
      hx-select-oob="#dialog-error"
    >
      <span>{{> icons/trash}}</span>
      {{t "common.delete"}}
    </button>
    {{> icons/spinner}}
  </div>
  <div id="dialog-error">
    {{#if error}}
      <p class="error">{{error}}</p>
    {{/if}}
  </div>
</div>
